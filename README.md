# SI507-Project

- `caching_tmdb.py`: code for caching the data from TMDB
- `youtube.py`: code for YouTube API usage
- `movie_graph.py`: code for organizing the cached data into graph
- `movies.json`: the cached data
